# DISABLED: This CI template is scaffolded but not yet active.
# To enable, remove all leading "# " prefixes from the lines below
# and commit. Enable after Phase 3 (integration smoke) passes locally.
# See plan §6 Follow-ups: "CI activation: uncomment after M3 passes locally."
#
# name: CI
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
#
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         python-version: ["3.11", "3.12"]
#
#     steps:
#       - uses: actions/checkout@v4
#
#       - name: Set up Conda
#         uses: conda-incubator/setup-miniconda@v3
#         with:
#           python-version: ${{ matrix.python-version }}
#           activate-environment: dl
#           environment-file: environment.yml
#           auto-activate-base: false
#
#       - name: Install system dependencies
#         run: |
#           sudo apt-get update -qq
#           sudo apt-get install -y pandoc latexmk texlive-latex-extra
#
#       - name: Install package in editable mode
#         shell: bash -l {0}
#         run: pip install -e ".[dev]"
#
#       - name: Lint with ruff
#         shell: bash -l {0}
#         run: |
#           ruff check litchron/ mcp_litchron/
#           ruff format --check litchron/ mcp_litchron/
#
#       - name: Type-check with mypy
#         shell: bash -l {0}
#         run: mypy --strict litchron/ mcp_litchron/
#
#       - name: Run tests (excluding live_llm marker)
#         shell: bash -l {0}
#         run: |
#           pytest tests/ \
#             -m "not live_llm" \
#             --cov=litchron \
#             --cov=mcp_litchron \
#             --cov-fail-under=80 \
#             -v
